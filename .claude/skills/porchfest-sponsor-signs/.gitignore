# Mirrored copy of dove.png, regenerated on first run by _bird_flip_path().
# Derived from a checked-in asset -- committing it would just be a second copy
# that silently goes stale if dove.png is ever replaced.
assets/.dove_flip.png
